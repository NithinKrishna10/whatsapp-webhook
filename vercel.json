{
    "builds": [{
        "src": "whatsapp-webhook/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "whatsapp-webhook/wsgi.py"
        }
    ]
}